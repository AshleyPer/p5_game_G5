function setup() {
  createCanvas(800, 800);
}

function draw() {
  background(255, 104, 100);
  textSize(32);
  text('This is not a test!', 300, height / 2)
  text('This is Jodie!', 300, height / 2 + 150)
}
